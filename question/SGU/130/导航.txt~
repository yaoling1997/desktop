有点像catalan数
然而直接dp即可
用点数表示当前状态
选一个点(不妨设每次都选标号最小的点)
枚举切的方式即可
若当前状态点数为奇数,则这个状态方案数为0
f[o]= f[0]*f[o-2]+f[2]*f[o-4]+...f[o-2]*f[0]
这个复杂度是n^2的
catalan数有一阶递推关系
h(n)=(4n-2)/(n+1)*h(n-1)
或h(n)=C(2n,n)/(n+1)
可以优化到O(n)
不会证怎么变到一阶的
