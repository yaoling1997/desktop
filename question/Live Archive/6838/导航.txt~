线段树维护
把'('看成1
')'看成-1
对于把'('改成')'  (1改成-1)
在它及其左边找一个最左的')',把它改成'('  (-1改成1)
对于把')'改成'('   (-1改成1)
在它左边找一个前缀和>=2,且到它是连续的>=2的位置
把'('改成')'
显然这个最左的位置上一定是'('
因为如果是')',向左一定可以找到'(',且前缀和>=2
然后改完后区间赋值(区间-2或+2)
