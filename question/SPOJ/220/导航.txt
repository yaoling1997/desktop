题意是给n个字符串
求在每个字符串中至少出现两次且不重叠的最长子串
n<=10,每个字符串长度<=10000
后缀数组处理多个串就是用不同的字符把这些小字符串连起来
然后求sa
求height
二分子串长度m
扫height
height>=m的视为一组
不在一组中的任何两个后缀的lcp都<m
然后看每组中是否有n个不同的串的后缀
且对于每个串的最右的出现位置-左的出现位置是否>=m
满足条件即m可行,否则不可行
