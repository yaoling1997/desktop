switch语句
给定两个正整数a,b以及一个运算符op,求对这两个数进行给定的运算。除法表示整除。
input:一行两个正整数a,b,接下来一个字符op表示运算(+-*/的一个)
output:一行一个整数,表示对应的答案
sample:
in: 
3 4 +
out:
7

switch(表达式){
case 常量表达式1:语句1
case 常量表达式2:语句2
case 常量表达式3:语句3
...
case 常量表达式n:语句n
default:语句n+1
}
先计算表达式的值,再匹配(看后面的常量表达式是否与上面的表达式相等),匹配成功后执行后面的语句,如果没有匹配则从default执行
每一个case的值必须互不相同
各个case和default的出现顺序可自由决定
多个case可共用一个语句
如
case 常量表达式x:
case 常量表达式x+1:
case 常量表达式x+2:语句x
......
每个语句后要加break;否则被匹配的语句后面的语句都会被执行而不会被跳过





