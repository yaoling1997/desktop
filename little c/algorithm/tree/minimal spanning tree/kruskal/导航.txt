x:
给个图求最小生成树权值
用边集数组存图，按边权从小到大排个序,依次考虑每条边(u,v),再用并查集维护，边能加则加，不能加就不加(不能形成环),直到有n-1条边为止
证明:
如果u和v在不同的联通分量中,加入(u,v)一定最优,因为如果不加这条边而加其它边得到一个解,那么再加入(u,v)会形成一个环,而且环上至少有一条边的权值大于或等于(u,v),删掉那条边的解不会比不加(u,v)的解更差

不要忘记排序...

BZOJ:1196 1016 1083
LA: 6837
