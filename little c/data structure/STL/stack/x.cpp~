#include<cstdio>
#include<cstdlib>
#include<stack>
using namespace std;
stack<int> g;
int main()
{
	g.push(1);
	if (!g.empty()) printf("yes\n");;
	printf("%d %d\n",g.size(),g.top());
	g.pop();
	return 0;
}
